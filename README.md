# Vetores
tipo[] nome = new tipo[tamanho]; Declara e cria um vetor vazio com um tamanho fixo.

tipo[] nome = {val1, val2, ...}; Declara e inicializa o vetor diretamente com valores.

vetor[indice] = valor; Insere ou altera o valor em uma posição específica (começando do 0).
